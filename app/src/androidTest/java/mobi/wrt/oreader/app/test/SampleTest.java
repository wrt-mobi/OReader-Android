package mobi.wrt.oreader.app.test;

import mobi.wrt.oreader.app.test.common.AbstractTestProcessor;

/**
 * Created by Uladzimir_Klyshevich on 4/23/2014.
 */
public class SampleTest extends AbstractTestProcessor {

  /*  public void testAudioFeeds() throws Exception {
        clear(SearchQueryAudio.class);
        clear(SearchAudio.class);
        testExecute(SearchAudioProcessor.SYSTEM_SERVICE_KEY, "audio.search.json?"+ Api.PARAM_OFFSET+"=0&"+ Api.PARAM_QUERY+"=skillet");
        checkCount(SearchAudio.class, 50);
        //TODO checkCount(WallItem.class, 200);
        checkCount(SearchQueryAudio.class, 50);
        Bundle args = new Bundle();
        args.putString(SearchManager.QUERY, "skillet");
        checkCount(new MusicSearchFragment(), args, 50);
    }
*/
}
